    /*
========================================
🏁 Kotlin Week 1 – Final Cumulative Challenges
========================================

You’ve learned the essentials:
✅ Variables (val, var), types
✅ String templates
✅ Functions & parameters
✅ Control flow (if, when, for, while)
✅ Null safety (?. and ?:)

Now let's combine it all.
*/

    fun weekOnePractice() {
        // 1. Write a function `greetUser` that takes a nullable name (String?)
        // If the name is null, print "Hello, guest!", otherwise print "Hello, [name]!"


        // 2. Create a function `ageCategory` that takes an age (Int) and returns a string:
        // - If age < 13 → "Child"
        // - 13..19 → "Teen"
        // - 20..64 → "Adult"
        // - 65+ → "Senior"


        // 3. Write a function `calculateTotalCost` that takes quantity (Int), price per item (Double),
        // and an optional discountPercent (Int = 0). Return the final cost using string templates.


        // 4. Use a loop to print numbers from 1 to 10. If a number is divisible by 3, print "Fizz" instead,
        // if divisible by 5, print "Buzz", and if both, print "FizzBuzz". Otherwise, print the number.


        // 5. Create a function `displayUserProfile` that takes name (nullable String?), age (nullable Int?), and city (nullable String?).
        // Print something like:
        // "Name: [name], Age: [age], City: [city]"
        // Use ?: to provide default values like "Unknown" or 0.


        // 6. Bonus: Create a `while` loop that keeps asking a user for input (mock with a loop counter or hardcoded list)
        // until they type "exit". Print a goodbye message at the end.


        // 7. Bonus: Create a function that takes a nullable list of tasks (List<String>?) and prints:
        // - "You have [x] tasks today." if not null.
        // - "No tasks for today!" if null or empty.
    }
